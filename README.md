This is a small Fractal Bitmap Creator.

## Build

In the repository root, run the following:

```
cmake .
cmake --build .
```

# Execute

In the repository root, run the following:

```
./bin/fractal_bitmap_creator 800 600 /path/to/ouptut.bmp
```